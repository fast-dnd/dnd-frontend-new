import React from "react";

import AdventureSkeletonLoading from "./components/adventure-skeleton-loading";

const Loading = () => {
  return <AdventureSkeletonLoading isEditing={false} />;
};

export default Loading;
